generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model calidades {
  id_calidad       BigInt             @id @default(autoincrement())
  nombre           String             @unique @db.VarChar(30)
  descripcion      String?
  estado           Boolean?           @default(true)
  created_at       DateTime?          @default(now()) @db.Timestamptz(6)
  detalle_carga    detalle_carga[]
  guias_operativas guias_operativas[]
  items_reparto    items_reparto[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model camiones {
  id_camion         BigInt              @id @default(autoincrement())
  placa             String              @unique @db.VarChar(10)
  marca             String?             @db.VarChar(50)
  color             String?             @db.VarChar(30)
  tipo_propiedad    String?             @db.VarChar(20)
  estado            Boolean?            @default(true)
  observaciones     String?
  created_at        DateTime?           @default(now()) @db.Timestamptz(6)
  gastos_operacion  gastos_operacion[]
  operaciones_carga operaciones_carga[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model cliente_sede {
  id_cliente_sede BigInt         @id @default(autoincrement())
  id_cliente      BigInt
  id_sede         BigInt
  id_rol_cliente  BigInt
  fecha_inicio    DateTime?      @default(dbgenerated("CURRENT_DATE")) @db.Date
  fecha_fin       DateTime?      @db.Date
  observaciones   String?
  estado          Boolean?       @default(true)
  created_at      DateTime?      @default(now()) @db.Timestamptz(6)
  clientes        clientes       @relation(fields: [id_cliente], references: [id_cliente], onDelete: Cascade, onUpdate: NoAction)
  roles_clientes  roles_clientes @relation(fields: [id_rol_cliente], references: [id_rol_cliente], onDelete: NoAction, onUpdate: NoAction)
  sedes           sedes          @relation(fields: [id_sede], references: [id_sede], onDelete: Cascade, onUpdate: NoAction)

  @@unique([id_cliente, id_sede, id_rol_cliente])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model clientes {
  id_cliente                                                   BigInt                      @id @default(autoincrement())
  nombre                                                       String                      @db.VarChar(200)
  documento                                                    String?                     @db.VarChar(20)
  telefono                                                     String?                     @db.VarChar(20)
  email                                                        String?                     @db.VarChar(100)
  direccion                                                    String?
  estado                                                       Boolean?                    @default(true)
  created_at                                                   DateTime?                   @default(now()) @db.Timestamptz(6)
  cliente_sede                                                 cliente_sede[]
  detalle_carga                                                detalle_carga[]
  entregas                                                     entregas[]
  facturas                                                     facturas[]
  guias_operativas                                             guias_operativas[]
  guias_remision_guias_remision_id_cliente_emisorToclientes    guias_remision[]            @relation("guias_remision_id_cliente_emisorToclientes")
  guias_remision_guias_remision_id_cliente_receptorToclientes  guias_remision[]            @relation("guias_remision_id_cliente_receptorToclientes")
  inventario_jabas_clientes                                    inventario_jabas_clientes[]
  movimientos_jabas                                            movimientos_jabas[]
  puesto_secciones                                             puesto_secciones[]
  puestos                                                      puestos[]
  reclamos                                                     reclamos[]
  vales_retorno_vales_retorno_id_cliente_destinoToclientes     vales_retorno[]             @relation("vales_retorno_id_cliente_destinoToclientes")
  vales_retorno_vales_retorno_id_cliente_solicitanteToclientes vales_retorno[]             @relation("vales_retorno_id_cliente_solicitanteToclientes")

  @@index([documento], map: "idx_clientes_documento")
  @@index([nombre], map: "idx_clientes_nombre")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model cobros_flete {
  id_cobro      BigInt    @id @default(autoincrement())
  id_entrega    BigInt
  monto_total   Decimal   @db.Decimal(10, 2)
  metodo_pago   String?   @db.VarChar(30)
  fecha_cobro   DateTime? @db.Date
  estado_pago   String?   @default("pendiente") @db.VarChar(20)
  comprobante   String?
  observaciones String?
  factura_id    BigInt?
  facturas      facturas? @relation(fields: [factura_id], references: [id_factura], onDelete: NoAction, onUpdate: NoAction)
  entregas      entregas  @relation(fields: [id_entrega], references: [id_entrega], onDelete: Cascade, onUpdate: NoAction)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model detalle_carga {
  id_detalle_carga      BigInt                  @id @default(autoincrement())
  id_operacion          BigInt
  id_cliente_emisor     BigInt
  id_fruta              BigInt
  id_variedad           BigInt?
  id_calidad            BigInt?
  id_tipo_jaba          BigInt
  cantidad_jabas        Int
  es_reparto_pendiente  Boolean?                @default(false)
  instruccion_reparto   String?
  precio_por_jaba       Decimal?                @db.Decimal(10, 2)
  observaciones         String?
  created_at            DateTime?               @default(now()) @db.Timestamptz(6)
  calidades             calidades?              @relation(fields: [id_calidad], references: [id_calidad], onDelete: NoAction, onUpdate: NoAction)
  clientes              clientes                @relation(fields: [id_cliente_emisor], references: [id_cliente], onDelete: NoAction, onUpdate: NoAction)
  frutas                frutas                  @relation(fields: [id_fruta], references: [id_fruta], onDelete: NoAction, onUpdate: NoAction)
  operaciones_carga     operaciones_carga       @relation(fields: [id_operacion], references: [id_operacion], onDelete: Cascade, onUpdate: NoAction)
  tipos_jaba            tipos_jaba              @relation(fields: [id_tipo_jaba], references: [id_tipo_jaba], onDelete: NoAction, onUpdate: NoAction)
  variedades            variedades?             @relation(fields: [id_variedad], references: [id_variedad], onDelete: NoAction, onUpdate: NoAction)
  guias_operativas      guias_operativas[]
  instrucciones_reparto instrucciones_reparto[]
  items_reparto         items_reparto[]

  @@index([id_cliente_emisor], map: "idx_detalle_emisor")
  @@index([id_operacion], map: "idx_detalle_operacion")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model entregas {
  id_entrega             BigInt           @id @default(autoincrement())
  id_guia                BigInt
  id_item_reparto        BigInt?
  id_cliente_receptor    BigInt
  id_puesto              BigInt
  id_empleado_entregador BigInt?
  fecha_entrega          DateTime         @db.Date
  hora_entrega           DateTime?        @db.Time(6)
  cantidad_entregada     Int
  cantidad_rechazada     Int              @default(0)
  estado_entrega         String           @db.VarChar(20)
  firma_recibido         Boolean?         @default(false)
  observaciones          String?
  created_at             DateTime?        @default(now()) @db.Timestamptz(6)
  cobros_flete           cobros_flete[]
  clientes               clientes         @relation(fields: [id_cliente_receptor], references: [id_cliente], onDelete: NoAction, onUpdate: NoAction)
  usuarios               usuarios?        @relation(fields: [id_empleado_entregador], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)
  guias_operativas       guias_operativas @relation(fields: [id_guia], references: [id_guia], onDelete: Cascade, onUpdate: NoAction)
  items_reparto          items_reparto?   @relation(fields: [id_item_reparto], references: [id_item_reparto], onDelete: NoAction, onUpdate: NoAction)
  puestos                puestos          @relation(fields: [id_puesto], references: [id_puesto], onDelete: NoAction, onUpdate: NoAction)
  reclamos               reclamos[]

  @@index([id_cliente_receptor], map: "idx_entregas_cliente")
  @@index([fecha_entrega], map: "idx_entregas_fecha")
  @@index([id_guia], map: "idx_entregas_guia")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model evidencias {
  id_evidencia  BigInt    @id @default(autoincrement())
  tipo_entidad  String    @db.VarChar(30)
  id_entidad    BigInt
  url_archivo   String
  descripcion   String?
  subido_por    BigInt?
  fecha_subida  DateTime? @default(now()) @db.Timestamptz(6)
  observaciones String?
  usuarios      usuarios? @relation(fields: [subido_por], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)

  @@index([tipo_entidad, id_entidad], map: "idx_evidencias_entidad")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model factura_items {
  id_item         BigInt   @id @default(autoincrement())
  id_factura      BigInt
  descripcion     String
  cantidad        Decimal  @db.Decimal(12, 3)
  unidad_medida   String?  @default("UND") @db.VarChar(10)
  precio_unitario Decimal  @db.Decimal(12, 2)
  valor_venta     Decimal  @db.Decimal(12, 2)
  igv             Decimal  @db.Decimal(12, 2)
  total           Decimal  @db.Decimal(12, 2)
  facturas        facturas @relation(fields: [id_factura], references: [id_factura], onDelete: Cascade, onUpdate: NoAction)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model facturas {
  id_factura                BigInt              @id @default(autoincrement())
  tipo                      String              @default("factura") @db.VarChar(20)
  id_serie                  BigInt
  numero                    String              @unique @db.VarChar(20)
  fecha_emision             DateTime            @db.Date
  fecha_vencimiento         DateTime?           @db.Date
  id_cliente                BigInt
  documento_cliente         String              @db.VarChar(20)
  razon_social              String              @db.VarChar(200)
  direccion                 String?
  total_gravada             Decimal             @db.Decimal(12, 2)
  total_igv                 Decimal             @db.Decimal(12, 2)
  total_otros_cargos        Decimal?            @db.Decimal(12, 2)
  total                     Decimal             @db.Decimal(12, 2)
  moneda                    String?             @default("PEN") @db.VarChar(3)
  estado                    String?             @default("emitido") @db.VarChar(20)
  sunat_codigo              String?             @db.VarChar(50)
  sunat_response            Json?
  id_vendedor               BigInt?
  id_comprobante_referencia BigInt?
  observaciones             String?
  created_at                DateTime?           @default(now()) @db.Timestamptz(6)
  updated_at                DateTime?           @default(now()) @db.Timestamptz(6)
  cobros_flete              cobros_flete[]
  factura_items             factura_items[]
  clientes                  clientes            @relation(fields: [id_cliente], references: [id_cliente], onDelete: NoAction, onUpdate: NoAction)
  facturas                  facturas?           @relation("facturasTofacturas", fields: [id_comprobante_referencia], references: [id_factura], onDelete: NoAction, onUpdate: NoAction)
  other_facturas            facturas[]          @relation("facturasTofacturas")
  series_comprobantes       series_comprobantes @relation(fields: [id_serie], references: [id_serie], onDelete: NoAction, onUpdate: NoAction)
  usuarios                  usuarios?           @relation(fields: [id_vendedor], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)
  guias_remision            guias_remision[]

  @@index([id_cliente], map: "idx_facturas_cliente")
  @@index([fecha_emision], map: "idx_facturas_fecha")
  @@index([numero], map: "idx_facturas_numero")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model frutas {
  id_fruta         BigInt             @id @default(autoincrement())
  nombre           String             @unique @db.VarChar(50)
  descripcion      String?
  estado           Boolean?           @default(true)
  created_at       DateTime?          @default(now()) @db.Timestamptz(6)
  detalle_carga    detalle_carga[]
  guias_operativas guias_operativas[]
  variedades       variedades[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model gastos_operacion {
  id_gasto          BigInt             @id @default(autoincrement())
  fecha_gasto       DateTime           @db.Date
  id_operacion      BigInt?
  id_camion         BigInt?
  concepto          String             @db.VarChar(100)
  monto             Decimal            @db.Decimal(10, 2)
  tipo_gasto        String             @db.VarChar(30)
  comprobante       String?
  observaciones     String?
  camiones          camiones?          @relation(fields: [id_camion], references: [id_camion], onDelete: NoAction, onUpdate: NoAction)
  operaciones_carga operaciones_carga? @relation(fields: [id_operacion], references: [id_operacion], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model guia_remision_items {
  id_item          BigInt         @id @default(autoincrement())
  id_guia_remision BigInt
  descripcion      String
  cantidad         Decimal        @db.Decimal(12, 3)
  unidad_medida    String?        @default("UND") @db.VarChar(10)
  peso_kg          Decimal?       @db.Decimal(12, 2)
  guias_remision   guias_remision @relation(fields: [id_guia_remision], references: [id_guia_remision], onDelete: Cascade, onUpdate: NoAction)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model guias_operativas {
  id_guia            BigInt        @id @default(autoincrement())
  numero_guia        String        @unique @db.VarChar(50)
  fecha_emision      DateTime      @db.Date
  id_detalle_carga   BigInt
  id_repartidor      BigInt?
  id_cliente_emisor  BigInt
  id_puesto_destino  BigInt
  id_fruta           BigInt
  id_variedad        BigInt?
  id_calidad         BigInt?
  id_tipo_jaba       BigInt
  cantidad_guia      Int
  precio_unitario    Decimal?      @db.Decimal(10, 2)
  fecha_salida_carga DateTime?     @db.Date
  vehiculo_placa     String?       @db.VarChar(10)
  observaciones      String?
  created_at         DateTime?     @default(now()) @db.Timestamptz(6)
  entregas           entregas[]
  calidades          calidades?    @relation(fields: [id_calidad], references: [id_calidad], onDelete: NoAction, onUpdate: NoAction)
  clientes           clientes      @relation(fields: [id_cliente_emisor], references: [id_cliente], onDelete: NoAction, onUpdate: NoAction)
  detalle_carga      detalle_carga @relation(fields: [id_detalle_carga], references: [id_detalle_carga], onDelete: NoAction, onUpdate: NoAction)
  frutas             frutas        @relation(fields: [id_fruta], references: [id_fruta], onDelete: NoAction, onUpdate: NoAction)
  puestos            puestos       @relation(fields: [id_puesto_destino], references: [id_puesto], onDelete: NoAction, onUpdate: NoAction)
  usuarios           usuarios?     @relation(fields: [id_repartidor], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)
  tipos_jaba         tipos_jaba    @relation(fields: [id_tipo_jaba], references: [id_tipo_jaba], onDelete: NoAction, onUpdate: NoAction)
  variedades         variedades?   @relation(fields: [id_variedad], references: [id_variedad], onDelete: NoAction, onUpdate: NoAction)
  incidencias        incidencias[]

  @@index([id_detalle_carga], map: "idx_guias_detalle")
  @@index([numero_guia], map: "idx_guias_numero")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model guias_remision {
  id_guia_remision                                      BigInt                @id @default(autoincrement())
  numero                                                String                @unique @db.VarChar(20)
  fecha_emision                                         DateTime              @db.Date
  fecha_traslado                                        DateTime              @db.Date
  id_cliente_emisor                                     BigInt
  id_cliente_receptor                                   BigInt
  punto_partida                                         String
  punto_llegada                                         String
  id_operacion                                          BigInt?
  id_factura                                            BigInt?
  estado                                                String?               @default("emitido") @db.VarChar(20)
  sunat_response                                        Json?
  created_at                                            DateTime?             @default(now()) @db.Timestamptz(6)
  guia_remision_items                                   guia_remision_items[]
  clientes_guias_remision_id_cliente_emisorToclientes   clientes              @relation("guias_remision_id_cliente_emisorToclientes", fields: [id_cliente_emisor], references: [id_cliente], onDelete: NoAction, onUpdate: NoAction)
  clientes_guias_remision_id_cliente_receptorToclientes clientes              @relation("guias_remision_id_cliente_receptorToclientes", fields: [id_cliente_receptor], references: [id_cliente], onDelete: NoAction, onUpdate: NoAction)
  facturas                                              facturas?             @relation(fields: [id_factura], references: [id_factura], onDelete: NoAction, onUpdate: NoAction)
  operaciones_carga                                     operaciones_carga?    @relation(fields: [id_operacion], references: [id_operacion], onDelete: NoAction, onUpdate: NoAction)

  @@index([numero], map: "idx_guias_remision_numero")
  @@index([id_operacion], map: "idx_guias_remision_operacion")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model incidencias {
  id_incidencia       BigInt             @id @default(autoincrement())
  fecha_incidencia    DateTime           @db.Date
  hora_incidencia     DateTime?          @db.Time(6)
  id_operacion        BigInt?
  id_guia             BigInt?
  tipo_incidencia     String             @db.VarChar(40)
  descripcion         String
  accion_tomada       String?
  id_empleado_reporta BigInt?
  created_at          DateTime?          @default(now()) @db.Timestamptz(6)
  usuarios            usuarios?          @relation(fields: [id_empleado_reporta], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)
  guias_operativas    guias_operativas?  @relation(fields: [id_guia], references: [id_guia], onDelete: NoAction, onUpdate: NoAction)
  operaciones_carga   operaciones_carga? @relation(fields: [id_operacion], references: [id_operacion], onDelete: NoAction, onUpdate: NoAction)

  @@index([id_operacion], map: "idx_incidencias_operacion")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model instrucciones_reparto {
  id_instruccion    BigInt            @id @default(autoincrement())
  id_operacion      BigInt
  id_detalle_carga  BigInt?
  medio_recepcion   String?           @db.VarChar(30)
  texto_instruccion String
  fecha_instruccion DateTime?         @default(now()) @db.Timestamptz(6)
  es_utilizada      Boolean?          @default(false)
  observaciones     String?
  detalle_carga     detalle_carga?    @relation(fields: [id_detalle_carga], references: [id_detalle_carga], onDelete: NoAction, onUpdate: NoAction)
  operaciones_carga operaciones_carga @relation(fields: [id_operacion], references: [id_operacion], onDelete: Cascade, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model inventario_jabas_clientes {
  id_inventario        BigInt     @id @default(autoincrement())
  id_cliente           BigInt
  id_tipo_jaba         BigInt
  cantidad_actual      Int        @default(0)
  color_marca          String?    @db.VarChar(100)
  ultima_actualizacion DateTime?  @default(now()) @db.Timestamptz(6)
  clientes             clientes   @relation(fields: [id_cliente], references: [id_cliente], onDelete: NoAction, onUpdate: NoAction)
  tipos_jaba           tipos_jaba @relation(fields: [id_tipo_jaba], references: [id_tipo_jaba], onDelete: NoAction, onUpdate: NoAction)

  @@unique([id_cliente, id_tipo_jaba])
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model items_reparto {
  id_item_reparto     BigInt        @id @default(autoincrement())
  id_detalle_carga    BigInt
  id_puesto           BigInt
  cantidad_jabas      Int
  id_calidad_override BigInt?
  orden_entrega       Int?
  estado_entrega      String?       @default("pendiente") @db.VarChar(20)
  observaciones       String?
  created_at          DateTime?     @default(now()) @db.Timestamptz(6)
  entregas            entregas[]
  calidades           calidades?    @relation(fields: [id_calidad_override], references: [id_calidad], onDelete: NoAction, onUpdate: NoAction)
  detalle_carga       detalle_carga @relation(fields: [id_detalle_carga], references: [id_detalle_carga], onDelete: Cascade, onUpdate: NoAction)
  puestos             puestos       @relation(fields: [id_puesto], references: [id_puesto], onDelete: NoAction, onUpdate: NoAction)

  @@index([id_detalle_carga], map: "idx_items_detalle")
  @@index([id_puesto], map: "idx_items_puesto")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model logs_actividad {
  id_log           BigInt    @id @default(autoincrement())
  id_usuario       BigInt?
  accion           String    @db.VarChar(50)
  tabla_afectada   String?   @db.VarChar(50)
  id_registro      BigInt?
  datos_anteriores Json?
  datos_nuevos     Json?
  ip               String?   @db.Inet
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  usuarios         usuarios? @relation(fields: [id_usuario], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)

  @@index([created_at], map: "idx_logs_fecha")
  @@index([id_usuario], map: "idx_logs_usuario")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mercados {
  id_mercado    BigInt    @id @default(autoincrement())
  id_sede       BigInt
  nombre        String    @db.VarChar(100)
  direccion     String?
  observaciones String?
  estado        Boolean?  @default(true)
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
  sedes         sedes     @relation(fields: [id_sede], references: [id_sede], onDelete: Cascade, onUpdate: NoAction)
  puestos       puestos[]

  @@unique([id_sede, nombre])
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model movimientos_jabas {
  id_movimiento              BigInt         @id @default(autoincrement())
  id_vale                    BigInt?
  id_tipo_jaba               BigInt
  cantidad                   Int
  fecha_movimiento           DateTime       @db.Date
  tipo_movimiento            String         @db.VarChar(30)
  id_empleado_responsable    BigInt?
  id_cliente_propietario     BigInt?
  estado_jaba                String?        @db.VarChar(20)
  descripcion_identificacion String?
  observaciones              String?
  created_at                 DateTime?      @default(now()) @db.Timestamptz(6)
  clientes                   clientes?      @relation(fields: [id_cliente_propietario], references: [id_cliente], onDelete: NoAction, onUpdate: NoAction)
  usuarios                   usuarios?      @relation(fields: [id_empleado_responsable], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)
  tipos_jaba                 tipos_jaba     @relation(fields: [id_tipo_jaba], references: [id_tipo_jaba], onDelete: NoAction, onUpdate: NoAction)
  vales_retorno              vales_retorno? @relation(fields: [id_vale], references: [id_vale], onUpdate: NoAction)

  @@index([id_cliente_propietario], map: "idx_movimientos_cliente")
  @@index([id_vale], map: "idx_movimientos_vale")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model operaciones_carga {
  id_operacion                                                BigInt                  @id @default(autoincrement())
  id_sede_origen                                              BigInt
  id_camion                                                   BigInt
  id_chofer                                                   BigInt?
  id_encargado_carga                                          BigInt?
  id_repartidor_asignado                                      BigInt?
  fecha_carga                                                 DateTime                @db.Date
  hora_carga                                                  DateTime?               @db.Time(6)
  estado                                                      String?                 @default("pendiente") @db.VarChar(20)
  observaciones                                               String?
  created_at                                                  DateTime?               @default(now()) @db.Timestamptz(6)
  detalle_carga                                               detalle_carga[]
  gastos_operacion                                            gastos_operacion[]
  guias_remision                                              guias_remision[]
  incidencias                                                 incidencias[]
  instrucciones_reparto                                       instrucciones_reparto[]
  camiones                                                    camiones                @relation(fields: [id_camion], references: [id_camion], onDelete: NoAction, onUpdate: NoAction)
  usuarios_operaciones_carga_id_choferTousuarios              usuarios?               @relation("operaciones_carga_id_choferTousuarios", fields: [id_chofer], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)
  usuarios_operaciones_carga_id_encargado_cargaTousuarios     usuarios?               @relation("operaciones_carga_id_encargado_cargaTousuarios", fields: [id_encargado_carga], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)
  usuarios_operaciones_carga_id_repartidor_asignadoTousuarios usuarios?               @relation("operaciones_carga_id_repartidor_asignadoTousuarios", fields: [id_repartidor_asignado], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)
  sedes                                                       sedes                   @relation(fields: [id_sede_origen], references: [id_sede], onDelete: NoAction, onUpdate: NoAction)
  pagos_empleados                                             pagos_empleados[]
  reclamos                                                    reclamos[]

  @@index([id_camion], map: "idx_operaciones_camion")
  @@index([estado], map: "idx_operaciones_estado")
  @@index([fecha_carga], map: "idx_operaciones_fecha")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model pagos_empleados {
  id_pago_empleado  BigInt             @id @default(autoincrement())
  id_empleado       BigInt
  id_operacion      BigInt?
  concepto          String?            @db.VarChar(100)
  monto             Decimal            @db.Decimal(10, 2)
  fecha_pago        DateTime?          @db.Date
  estado_pago       String?            @default("pendiente") @db.VarChar(20)
  usuarios          usuarios           @relation(fields: [id_empleado], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)
  operaciones_carga operaciones_carga? @relation(fields: [id_operacion], references: [id_operacion], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model puesto_secciones {
  id_seccion     BigInt    @id @default(autoincrement())
  id_puesto      BigInt
  id_cliente     BigInt?
  codigo_seccion String    @db.VarChar(50)
  descripcion    String?
  observaciones  String?
  estado         Boolean?  @default(true)
  created_at     DateTime? @default(now()) @db.Timestamptz(6)
  clientes       clientes? @relation(fields: [id_cliente], references: [id_cliente], onDelete: NoAction, onUpdate: NoAction)
  puestos        puestos   @relation(fields: [id_puesto], references: [id_puesto], onDelete: Cascade, onUpdate: NoAction)

  @@unique([id_puesto, codigo_seccion])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model puestos {
  id_puesto        BigInt             @id @default(autoincrement())
  id_mercado       BigInt
  id_cliente       BigInt?
  numero_puesto    String             @db.VarChar(50)
  referencia       String?
  es_principal     Boolean?           @default(false)
  estado           Boolean?           @default(true)
  created_at       DateTime?          @default(now()) @db.Timestamptz(6)
  entregas         entregas[]
  guias_operativas guias_operativas[]
  items_reparto    items_reparto[]
  puesto_secciones puesto_secciones[]
  clientes         clientes?          @relation(fields: [id_cliente], references: [id_cliente], onDelete: NoAction, onUpdate: NoAction)
  mercados         mercados           @relation(fields: [id_mercado], references: [id_mercado], onDelete: Cascade, onUpdate: NoAction)
  vales_retorno    vales_retorno[]

  @@unique([id_mercado, numero_puesto])
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model reclamos {
  id_reclamo           BigInt             @id @default(autoincrement())
  fecha_reclamo        DateTime           @db.Date
  id_cliente_afectado  BigInt
  id_operacion         BigInt?
  id_entrega           BigInt?
  tipo_reclamo         String             @db.VarChar(30)
  descripcion          String
  monto_reclamado      Decimal?           @db.Decimal(10, 2)
  estado_reclamo       String?            @default("abierto") @db.VarChar(20)
  resolucion           String?
  fecha_resolucion     DateTime?          @db.Date
  id_empleado_gestiona BigInt?
  created_at           DateTime?          @default(now()) @db.Timestamptz(6)
  clientes             clientes           @relation(fields: [id_cliente_afectado], references: [id_cliente], onDelete: NoAction, onUpdate: NoAction)
  usuarios             usuarios?          @relation(fields: [id_empleado_gestiona], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)
  entregas             entregas?          @relation(fields: [id_entrega], references: [id_entrega], onDelete: NoAction, onUpdate: NoAction)
  operaciones_carga    operaciones_carga? @relation(fields: [id_operacion], references: [id_operacion], onDelete: NoAction, onUpdate: NoAction)

  @@index([id_cliente_afectado], map: "idx_reclamos_cliente")
  @@index([estado_reclamo], map: "idx_reclamos_estado")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model reportes_programados {
  id_reporte        BigInt    @id @default(autoincrement())
  nombre            String    @db.VarChar(100)
  descripcion       String?
  query_sql         String
  formato           String    @db.VarChar(10)
  periodicidad      String    @db.VarChar(20)
  hora_programacion DateTime? @db.Time(6)
  destinatarios     String[]
  ultima_ejecucion  DateTime? @db.Timestamptz(6)
  estado            Boolean?  @default(true)
  created_at        DateTime? @default(now()) @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model roles_clientes {
  id_rol_cliente BigInt         @id @default(autoincrement())
  nombre         String         @unique @db.VarChar(50)
  descripcion    String?
  estado         Boolean?       @default(true)
  created_at     DateTime?      @default(now()) @db.Timestamptz(6)
  cliente_sede   cliente_sede[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model roles_usuarios {
  id_rol_usuario BigInt     @id @default(autoincrement())
  nombre         String     @unique @db.VarChar(50)
  descripcion    String?
  estado         Boolean?   @default(true)
  created_at     DateTime?  @default(now()) @db.Timestamptz(6)
  usuarios       usuarios[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sedes {
  id_sede           BigInt              @id @default(autoincrement())
  nombre            String              @db.VarChar(100)
  tipo_sede         String?             @db.VarChar(20)
  direccion         String?
  ciudad            String?             @db.VarChar(50)
  departamento      String?             @db.VarChar(50)
  telefono          String?             @db.VarChar(20)
  estado            Boolean?            @default(true)
  created_at        DateTime?           @default(now()) @db.Timestamptz(6)
  cliente_sede      cliente_sede[]
  mercados          mercados[]
  operaciones_carga operaciones_carga[]
  usuarios          usuarios[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model series_comprobantes {
  id_serie           BigInt     @id @default(autoincrement())
  tipo               String     @db.VarChar(20)
  serie              String     @db.VarChar(4)
  correlativo_actual Int        @default(1)
  estado             Boolean?   @default(true)
  created_at         DateTime?  @default(now()) @db.Timestamptz(6)
  facturas           facturas[]

  @@unique([tipo, serie])
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model tipos_jaba {
  id_tipo_jaba              BigInt                      @id @default(autoincrement())
  nombre                    String                      @unique @db.VarChar(50)
  tipo_material             String?                     @db.VarChar(20)
  capacidad_aprox           String?                     @db.VarChar(50)
  flete_base                Decimal?                    @db.Decimal(10, 2)
  estado                    Boolean?                    @default(true)
  created_at                DateTime?                   @default(now()) @db.Timestamptz(6)
  detalle_carga             detalle_carga[]
  guias_operativas          guias_operativas[]
  inventario_jabas_clientes inventario_jabas_clientes[]
  movimientos_jabas         movimientos_jabas[]
  vales_retorno             vales_retorno[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model usuarios {
  id_usuario                                                           BigInt              @id @default(autoincrement())
  id_sede                                                              BigInt
  id_rol_usuario                                                       BigInt
  nombres                                                              String              @db.VarChar(100)
  apellidos                                                            String?             @db.VarChar(100)
  telefono                                                             String?             @db.VarChar(20)
  email                                                                String?             @unique @db.VarChar(100)
  password_hash                                                        String?
  estado                                                               Boolean?            @default(true)
  created_at                                                           DateTime?           @default(now()) @db.Timestamptz(6)
  google_id                                                            String?             @unique @db.VarChar(255)
  avatar_url                                                           String?
  estado_acceso                                                        String?             @default("activo") @db.VarChar(20)
  entregas                                                             entregas[]
  evidencias                                                           evidencias[]
  facturas                                                             facturas[]
  guias_operativas                                                     guias_operativas[]
  incidencias                                                          incidencias[]
  logs_actividad                                                       logs_actividad[]
  movimientos_jabas                                                    movimientos_jabas[]
  operaciones_carga_operaciones_carga_id_choferTousuarios              operaciones_carga[] @relation("operaciones_carga_id_choferTousuarios")
  operaciones_carga_operaciones_carga_id_encargado_cargaTousuarios     operaciones_carga[] @relation("operaciones_carga_id_encargado_cargaTousuarios")
  operaciones_carga_operaciones_carga_id_repartidor_asignadoTousuarios operaciones_carga[] @relation("operaciones_carga_id_repartidor_asignadoTousuarios")
  pagos_empleados                                                      pagos_empleados[]
  reclamos                                                             reclamos[]
  roles_usuarios                                                       roles_usuarios      @relation(fields: [id_rol_usuario], references: [id_rol_usuario], onDelete: NoAction, onUpdate: NoAction)
  sedes                                                                sedes               @relation(fields: [id_sede], references: [id_sede], onDelete: NoAction, onUpdate: NoAction)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model vales_retorno {
  id_vale                                                 BigInt              @id @default(autoincrement())
  numero_vale                                             String              @unique @db.VarChar(50)
  fecha_emision                                           DateTime            @db.Date
  id_cliente_solicitante                                  BigInt
  id_cliente_destino                                      BigInt
  id_puesto_origen                                        BigInt?
  id_tipo_jaba                                            BigInt
  cantidad_solicitada                                     Int
  cantidad_entregada                                      Int                 @default(0)
  estado_jaba_requerido                                   String?             @db.VarChar(20)
  fecha_canje                                             DateTime?           @db.Date
  id_cajoneria                                            BigInt?
  observaciones                                           String?
  created_at                                              DateTime?           @default(now()) @db.Timestamptz(6)
  movimientos_jabas                                       movimientos_jabas[]
  clientes_vales_retorno_id_cliente_destinoToclientes     clientes            @relation("vales_retorno_id_cliente_destinoToclientes", fields: [id_cliente_destino], references: [id_cliente], onDelete: NoAction, onUpdate: NoAction)
  clientes_vales_retorno_id_cliente_solicitanteToclientes clientes            @relation("vales_retorno_id_cliente_solicitanteToclientes", fields: [id_cliente_solicitante], references: [id_cliente], onDelete: NoAction, onUpdate: NoAction)
  puestos                                                 puestos?            @relation(fields: [id_puesto_origen], references: [id_puesto], onDelete: NoAction, onUpdate: NoAction)
  tipos_jaba                                              tipos_jaba          @relation(fields: [id_tipo_jaba], references: [id_tipo_jaba], onDelete: NoAction, onUpdate: NoAction)

  @@index([id_cliente_destino], map: "idx_vales_cliente_destino")
  @@index([id_cliente_solicitante], map: "idx_vales_cliente_solicitante")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model variedades {
  id_variedad      BigInt             @id @default(autoincrement())
  id_fruta         BigInt
  nombre           String             @db.VarChar(50)
  descripcion      String?
  estado           Boolean?           @default(true)
  created_at       DateTime?          @default(now()) @db.Timestamptz(6)
  detalle_carga    detalle_carga[]
  guias_operativas guias_operativas[]
  frutas           frutas             @relation(fields: [id_fruta], references: [id_fruta], onDelete: Cascade, onUpdate: NoAction)
}
